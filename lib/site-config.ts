/**
 * Central SEO/site configuration.
 * Use this for canonical URLs, sitemap, robots, and structured data
 * so all URLs stay consistent and never use undefined.
 */
export const SITE_BASE_URL =
  process.env.NEXT_PUBLIC_SITE_URL || "https://www.studymitra.in";
